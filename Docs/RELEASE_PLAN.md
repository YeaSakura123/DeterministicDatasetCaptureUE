# 正式版交付核对

目标依据为项目根目录的 README、EXECUTION_PLAN、DATA_AND_ENGINE_CONTRACT，以及用户要求的插件功能完整性、GitHub 上传与 README 修正。当前使用已确定的 UE 5.7 / Windows / DX12。核对覆盖数据捕获插件的正式交付，后续 CNN、Transformer 与实时推理仍按根计划阶段推进。

下面的项目必须有实际证据，不能仅以配置项存在、版本号或历史 PASS 判定完成。

| 要求 | 来源 | 验证依据 | 当前状态 |
|---|---|---|---|
| 固定时钟、Sequencer、真实 Main View LR、原生 HR、Depth/Motion/曝光/jitter/矩阵 | 执行计划 3.1–3.2；数据契约 2、4–7 | 真实序列、解析几何、相机/物体/混合、同速抵消、cut、前后栅格 | 当前二进制 8 组共 472 帧核心回归通过；重复/反序各 1792 文件对字节一致；同速抵消 8 帧实测物体残差 <0.00364 px、背景解析位移误差 <0.000080 px |
| 固定时间的 16–64 子样本高质量 reference，独立 View History | 执行计划 3.3；数据契约 8 | 子样本时间与相机证据、平均结果、收敛/细线/Mip/材质对照、附加渲染不污染 LR | 已实现 1/16/32/64；冻结时间、16 样本独立面积积分与 LR 隔离通过；1080p 真实纹理的 16 样本相对 64 样本参考显著收敛 |
| 异步压缩/写盘、内存有界、失败传播、完成后才发布帧 | 执行计划 3.1 与 M1 验收 | 工作线程记录、与同步基准数值比较、限额回压及真实写入失败测试 | 32 MiB 回压、渲染线程零写盘、工作线程错误传播、清单发布检查与 3141 项同步/异步对比通过；PNG 少数像素量化差异保留，不声称渲染字节全同 |
| 可运行的质量门槛与数据版本准入 | 执行计划 M1 验收；数据契约 14 | 无 NaN/Inf、维度/颜色/深度/几何、旧报告/坏文件拒绝，失败数据不得进入正式索引 | v2 严格准入、源文件/报告/验证器哈希绑定及失败拒绝已实现；4 地图 32 帧和六项交付边界检查通过，全量数据逐片检查中 |
| 真实纹理、光照、基础遮挡/透明/动画/WPO、曝光和 UI 策略 | 执行计划 3.2–3.3；数据契约 3、4、6 | 原始输出、独立遮挡/参考图/曝光检查、前检及 UI 零残留 | 原创 4 地图、1080p 材质/reference 对照、遮挡/UI/透明夹具、项目骨骼/动态材质/WPO 通过；坏材质 fallback 在第 0 帧前失败；未知自遮挡和透明真值限制保留 |
| FG 完整双向端点、真实中间帧、UI 与隔离历史 | 执行计划 M1 验收；数据契约 10 | 正向、反向、中点独立进程及装配重验 | 携带 WPO、骨骼、动态材质原始证据的两套装配分别 199/199、192/192 通过限定范围准入；六种伪造/缺项负例全部拒绝，详见 FG_ACCEPTANCE |
| 多关卡/轨迹任务运行、按场景划分、版本清单与打包训练输入 | 执行计划 3.4；数据契约 9–10 | 可重复运行的任务队列，防帧泄漏索引，shard 读取与哈希 | 批量执行/复用、索引、tar 分片与读取通过；严格 v2 的 4 场景 32 帧通过时间训练准入并从 4 个分片全部读取；10 分钟批次采集中 |
| 至少 3 个训练关卡、1 个未见测试关卡、10–30 分钟验证数据 | 执行计划 M1 交付 | 实际 manifest、时长、划分、质量报告和源资产记录 | 30 × 600 帧批次正在实际采集与全量验证；未完成前不标记达到十分钟交付 |
| 插件安装/构建/打包可独立运行 | 用户正式版要求 | 干净插件包构建与安装测试 | rc.1 干净包构建成功，在无作者源代码/资产的空项目生成原创地图并完成 8 帧 1080p/ref16，1912/1912 通过；最终 1.0 包复验中 |
| README 是可执行的使用入口、功能及边界准确 | 用户明确要求 | 按 README 在干净包上执行、链接/路径核对 | 候选版入口已重写；干净包命令逐项验证中 |
| 上传插件 GitHub 并发布可下载交付物 | 用户明确要求 | 远端 commit、tag/release、资产下载校验 | 已上传候选源码和 README 至公共插件仓库 codex/formal-release，commit 1679f7a；后续修正、正式 tag/release 和下载校验待完成 |

不扩展 Chaos 内部求解器、任意 Niagara DI、Movie Render Graph 框架或新的通用适配层。现有可选能力保留其真实支持边界；是否完成以上核心要求由数据和运行证据决定。
